# Digital Human Job Runner App

This directory contains the runnable application:

- `backend/`: FastAPI API and local pipeline wrappers
- `frontend/`: React + Vite web console
- `config/`: job schema and example job documents

For setup and configuration, see the repository root `README.md`.
